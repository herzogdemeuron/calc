<Window x:Class="Calc.ConnectorRevit.Views.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Calc.ConnectorRevit.Views" 
        d:DataContext="{d:DesignInstance Type=local:ViewModel}"
        Loaded="Window_Loaded"
        mc:Ignorable="d" 
        Background="White"
        Height="600" MinHeight="600"
        Width="600" MinWidth="600"
        WindowStartupLocation="CenterScreen"
        Topmost="True">

    <Window.Resources>
        <local:HostTemplateSelector x:Key="HostTemplateSelector" />

        <DataTemplate x:Key="BuildupTemplate">
            <DockPanel LastChildFill="True">
                <Label Content="Assign Buildup:" DockPanel.Dock="Top"/>
                <ComboBox SelectedItem="{Binding Buildup}" 
                          ItemsSource="{Binding DataContext.AllBuildups, 
                                                RelativeSource={RelativeSource AncestorType={x:Type Window}}}" 
                          DisplayMemberPath="Name"
                          DockPanel.Dock="Top"/>
                <Label Content="Preview:" DockPanel.Dock="Top"/>
                <DataGrid ItemsSource="{Binding Buildup.Components}"
                          AutoGenerateColumns="False"
                          VerticalAlignment="Top"
                          MinHeight="100">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Material" Binding="{Binding Material.Name}" />
                        <DataGridTextColumn Header="Amount" Binding="{Binding Amount}" IsReadOnly="True" />
                    </DataGrid.Columns>
                </DataGrid>
            </DockPanel>
        </DataTemplate>

        <DataTemplate x:Key="RootTemplate">
            <DockPanel>
                <Label Content="Roots:"
                       DockPanel.Dock="Top"/>
                <DataGrid ItemsSource="{Binding Roots}"
                          AutoGenerateColumns="False"
                          VerticalAlignment="Top">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Parameter" Binding="{Binding Parameter}" />
                        <DataGridTextColumn Header="Value" Binding="{Binding Value}" IsReadOnly="True" />
                    </DataGrid.Columns>
                </DataGrid>
            </DockPanel>
        </DataTemplate>

        <DataTemplate x:Key="TreeTemplate">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="100*"/>
                    <RowDefinition Height="200*"/>
                </Grid.RowDefinitions>
                <ContentControl Grid.Row="0"
                                Content="{Binding}"
                                ContentTemplate="{StaticResource RootTemplate}" />
                <ContentControl Grid.Row="1"
                                Content="{Binding}"
                                ContentTemplate="{StaticResource BuildupTemplate}" />
            </Grid>
        </DataTemplate>

        <DataTemplate x:Key="BranchTemplate">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="100*"/>
                    <RowDefinition Height="200*"/>
                </Grid.RowDefinitions>
                <ContentControl Grid.Row="1"
                                Content="{Binding}"
                                ContentTemplate="{StaticResource BuildupTemplate}" />
            </Grid>
        </DataTemplate>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="20*"/>
            <ColumnDefinition Width="20*"/>
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="0"
              Margin="10">
            <TreeView x:Name="treeView"                  
                  ItemsSource="{Binding Items}"
                  SelectedItemChanged="TreeViewSelectedItemChanged">
                <TreeView.ItemTemplate>
                    <HierarchicalDataTemplate DataType="local:TreeViewItem" ItemsSource="{Binding Children}">
                        <TextBlock Text="{Binding NameString}" />
                    </HierarchicalDataTemplate>
                </TreeView.ItemTemplate>
            </TreeView>

            <Grid x:Name="LoadingOverlay"
              Background="LightGray">
                <TextBlock HorizontalAlignment="Center"
                       VerticalAlignment="Center" 
                       Text="LOADING..."/>
            </Grid>
        </Grid>

        <DockPanel Grid.Column="2"
                   Margin="10">
            <Button Content="Reset Visualization"
                    Height="30"
                    DockPanel.Dock="Bottom"
                    Click="ResetViewClicked" />
            <Button Content="Calculate"
                    Height="30"
                    Margin="0 0 0 10"
                    DockPanel.Dock="Bottom"
                    Click="CalculateClicked" />
            <ContentControl Content ="{Binding SelectedItem.Host}"
                            DockPanel.Dock="Top"
                            ContentTemplateSelector="{StaticResource HostTemplateSelector}"
                            BorderThickness="1"/>
        </DockPanel>

    </Grid>
</Window>