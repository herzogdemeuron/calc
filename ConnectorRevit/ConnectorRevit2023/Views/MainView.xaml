<Window x:Class="Calc.ConnectorRevit.Views.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Calc.ConnectorRevit.Views" 
        d:DataContext="{d:DesignInstance Type=local:MainViewModel}"
        Loaded="WindowLoaded"
        Closing="WindowClosing"
        mc:Ignorable="d" 
        Background="White"
        Height="600" MinHeight="600"
        Width="600" MinWidth="600"
        WindowStartupLocation="CenterScreen"
        Topmost="True">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="MainViewResources.xaml"/>
                <ResourceDictionary Source="ToggleButtonResource.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>
    <Grid>
        
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="20*"/>
                <ColumnDefinition Width="20"/>
                <ColumnDefinition Width="20*"/>
            </Grid.ColumnDefinitions>
            
            <Grid.RowDefinitions>
                <RowDefinition Height="50"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="40"/>
            </Grid.RowDefinitions>

            <DockPanel Grid.Column="0"
                       Grid.Row="0"
                       Margin="10"
                       HorizontalAlignment="Stretch">
                <Button Content="Load" Width="50" Style= "{StaticResource ButtonStyle}"/>
                <ComboBox x:Name="ForestsComboBox"
                            ItemsSource="{Binding DataContext.Forests, RelativeSource={RelativeSource AncestorType={x:Type Window}}}"
                          DisplayMemberPath="Name"
                          DockPanel.Dock="Left"
                          Height="25"
                          SelectionChanged="ForestSelectionChanged"
                          HorizontalAlignment="Stretch"/>
            </DockPanel>

            <DockPanel Grid.Column="2"
                       Grid.Row="0"
                       Margin="10"
                       HorizontalAlignment="Stretch">
                <Button Content="Update" 
                        Width="50" 
                        Click="UpdateMappingClicked"
                        Style= "{StaticResource ButtonStyle}"/>
                <Button Content="New" 
                        Width="50" 
                        Click="NewMappingClicked"
                        Style= "{StaticResource ButtonStyle}"/>
                <ComboBox x:Name="MappingsComboBox"
                          ItemsSource="{Binding DataContext.Mappings, RelativeSource={RelativeSource AncestorType={x:Type Window}}}"
                          DisplayMemberPath="Name"
                          DockPanel.Dock="Left"
                          Height="25"
                          SelectionChanged="MappingSelectionChanged"
                          HorizontalAlignment="Stretch"/>
            </DockPanel>

            <Grid Grid.Column="0"
                  Grid.Row="1"
                  Margin="10">
                <TreeView x:Name="TreeView"
                      ItemsSource="{Binding NodeSource}"
                      TreeViewItem.Selected="TreeViewItemSelected"
                      MouseLeftButtonDown="SideClickDown"
                      ItemContainerStyle="{StaticResource TreeViewItemStyle}">
                    <TreeView.ItemTemplate>
                        <HierarchicalDataTemplate ItemsSource="{Binding SubNodeItems}">
                            <ContentControl Content="{Binding}"
                                            
                                        ContentTemplate="{StaticResource BranchItemTemplate}">
                                <ContentControl.ToolTip>
                                    <TextBlock Text="{Binding Host.Buildup}"/>
                                </ContentControl.ToolTip>
                            </ContentControl>
                        </HierarchicalDataTemplate>
                    </TreeView.ItemTemplate>
                </TreeView>
            </Grid>
            
            <DockPanel  x:Name="BuildupPanel"
                        Grid.Column="2"
                        Grid.Row="1"
                        Margin="10"
                        LastChildFill="True"
                        DockPanel.Dock="Top">
                <Label Content="Assigned Buildup:" DockPanel.Dock="Top"/>
                <DockPanel HorizontalAlignment="Stretch"
                            DockPanel.Dock="Top"
                           LastChildFill="True"
                            >
                    <Button Content="From Parent"
                            Width="50"
                            Style= "{StaticResource ButtonStyle}"
                            Click="InheritClicked"/>
                    <ComboBox x:Name="BuildupsComboBox"
                              
                        ItemsSource="{Binding DataContext.AllBuildups, RelativeSource={RelativeSource AncestorType={x:Type Window}}}"
                        SelectedItem="{Binding DataContext.SelectedNodeItem.Host.Buildup, RelativeSource={RelativeSource AncestorType={x:Type Window}}, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                        DisplayMemberPath="Name"
                        SelectionChanged="BuildupSelectionChanged"/>
                    

                </DockPanel>
                
                <Label Content="Buildup Preview:" DockPanel.Dock="Top"/>
                <DataGrid ItemsSource="{Binding SelectedItem.Components, ElementName=BuildupsComboBox}"
                        AutoGenerateColumns="False"
                        VerticalAlignment="Top"
                        HorizontalAlignment="Stretch"
                        MinHeight="100">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Material" Binding="{Binding Material.Name}" />
                        <DataGridTextColumn Header="Amount" Binding="{Binding Amount}" IsReadOnly="True" />
                    </DataGrid.Columns>
                </DataGrid>
            </DockPanel>
            <StackPanel Grid.Column="0"
                        Grid.Row="2"
                        Orientation="Horizontal"
                        VerticalAlignment="Center"
                        HorizontalAlignment="Left"
                        Margin="10,0,0,10">
                <TextBlock Text="Branch" FontSize="10" Margin="0,2,4,0"/>
                <ToggleButton x:Name="ViewToggleButton"
                                  Style="{StaticResource ToggleButtonStyle}" 
                                  Checked="ViewToggleButtonChecked"
                                  Unchecked="ViewToggleButtonUnchecked"/>
                <TextBlock Text="Buildup" FontSize="10" Margin="4,2,0,0"/>
            </StackPanel>
            <Button Grid.Column="0"
                    Grid.Row="2"
                    Content="Update Revit"
                    Margin="0,0,10,10"
                    Width="90"
                    Click="UpdateClicked"
                    HorizontalAlignment="Right"
                    Style= "{StaticResource ButtonStyle}"/>

            <Grid Grid.Column="2"
                    Grid.Row="2">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Button Grid.Column="0" 
                        Content="Start CalcLive"
                        Click="StartCalcLiveClicked" Margin="10,0,5,10" />
                <Button Grid.Column="1"
                        Content="Save Results"
                        Click="SaveResultsClicked" Margin="5,0,10,10" />
            </Grid>

        </Grid>
        
        <Grid x:Name="SelectProjectOverlay"
              d:IsHidden="true"
          Background="White"
          Margin="10">
            <StackPanel VerticalAlignment="Center">
                <TextBlock HorizontalAlignment="Center"
                            Text="Choose Project:"
                           Margin="10"/>
                <ComboBox x:Name="ProjectsComboBox"
                          HorizontalAlignment="Center"
                          ItemsSource= "{Binding AllProjects}"
                          DisplayMemberPath="ProjectNumber"
                          Width="90"/>
                <Button Content="OK" 
                        HorizontalAlignment="Center"
                        Width="90"
                        Margin="10"
                        Click="ProjectOKClicked"/>
            </StackPanel>
        </Grid>
        
        <Grid x:Name="LoadingOverlay"
              d:IsHidden="true" 
          Background="LightGray"
          Margin="10">
            <TextBlock HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Text="Loading..."/>
        </Grid>

    </Grid>
</Window>